name: Build & Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # ── Windows x64 ──
          - platform: windows-x64
            os: windows-latest
            exe-suffix: .exe
          # ── Linux x64 ──
          - platform: linux-x64
            os: ubuntu-latest
            exe-suffix: ""
          # ── macOS ARM64 (Apple Silicon) ──
          - platform: macos-arm64
            os: macos-latest
            exe-suffix: ""

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Dart SDK
        uses: dart-lang/setup-dart@v1
        with:
          sdk: stable

      - name: Install dependencies
        run: dart pub get

      - name: Compile server
        run: dart compile exe bin/wenzagent_server.dart -o build/wenzagent_server${{ matrix.exe-suffix }}

      - name: Compile client
        run: dart compile exe bin/wenzagent_client.dart -o build/wenzagent_client${{ matrix.exe-suffix }}

      - name: Prepare package files
        shell: bash
        run: |
          cp doc/RELEASE_README.md build/README.md
          mkdir -p build/config
          cp config/wenzagent_server.yaml.example build/config/
          cp config/wenzagent_client.yaml.example build/config/

      - name: Package artifacts (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist
          Compress-Archive -Path build/* -DestinationPath dist/wenzagent-${{ matrix.platform }}.zip

      - name: Package artifacts (Linux/macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mkdir -p dist
          tar czf dist/wenzagent-${{ matrix.platform }}.tar.gz -C build .

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wenzagent-${{ matrix.platform }}
          path: dist/*
          retention-days: 1

  # ──────────────────────────────────────────────────────────────────────
  # Linux ARM64：使用 Dart 交叉编译
  # ──────────────────────────────────────────────────────────────────────
  build-linux-arm64:
    name: Build linux-arm64
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Dart SDK
        uses: dart-lang/setup-dart@v1
        with:
          sdk: stable

      - name: Install dependencies
        run: dart pub get

      - name: Compile server
        run: dart compile exe bin/wenzagent_server.dart --target-os=linux --target-arch=arm64 -o build/wenzagent_server

      - name: Compile client
        run: dart compile exe bin/wenzagent_client.dart --target-os=linux --target-arch=arm64 -o build/wenzagent_client

      - name: Prepare package files
        run: |
          cp doc/RELEASE_README.md build/README.md
          mkdir -p build/config
          cp config/wenzagent_server.yaml.example build/config/
          cp config/wenzagent_client.yaml.example build/config/

      - name: Package artifacts
        run: |
          mkdir -p dist
          tar czf dist/wenzagent-linux-arm64.tar.gz -C build .

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wenzagent-linux-arm64
          path: dist/*
          retention-days: 1

  release:
    name: Create GitHub Release
    needs: [build, build-linux-arm64]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: List artifacts
        run: ls -lh dist/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: dist/*
